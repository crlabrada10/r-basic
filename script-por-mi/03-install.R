#Comando para instalar librerías en R-Studio
install.packages("tidyverse", dependencies = TRUE)

#Comando para habilitar la librería
library(tidyverse)

install.packages("magic", dependencies = TRUE)

library(magic)

magic(6)

install.packages()
